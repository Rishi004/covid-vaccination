version: 2.1
orbs:
  browser-tools: circleci/browser-tools@1.2.3
jobs:
  build:
    docker:
      - image: cimg/php:8.1-browsers # The primary container where steps are run
        auth:
          username: mydockerhub-user
          password: $DOCKERHUB_PASSWORD  # context / project UI env-var reference
      - image: cimg/mysql:8.0
        auth:
          username: rishi004
          password: V$Rishi35686650  # context / project UI env-var reference
        environment:
          MYSQL_ROOT_PASSWORD: root
          MYSQL_DATABASE: covid-vaccination
          MYSQL_USER: root
          MYSQL_PASSWORD: root

    steps:
      - checkout
      - run:
      # Our primary container isn't MYSQL so run a sleep command until it's ready.
          name: Waiting for MySQL to be ready
          command: |
            for i in `seq 1 10`;
            do
              nc -z 127.0.0.1 3306 && echo Success && exit 0
              echo -n .
              sleep 1
            done
            echo Failed waiting for MySQL && exit 1
      - run:
          name: Install MySQL CLI; Import dummy data; run an example query
          command: |
            sudo apt-get install default-mysql-client
            mysql -h 127.0.0.1 -u root -p root test_db < sql-data/dummy.sql
            mysql -h 127.0.0.1 -u root -p root --execute="SELECT * FROM covid-vaccination.Persons"
workflows:
  build-deploy:
    jobs:
      - build
